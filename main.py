# Пример списка чисел
numbers = [1, 2, 3, 4, 5]
# Переменная для хранения суммы
total_sum = 0
# Цикл for для перебора всех чисел в списке
for num in numbers:
    # Добавление каждого числа к сумме
    total_sum += num
# Вывод общ суммы
print(f"Сумма всех чисел в списке: {total_sum}")
